services:
  backend:
    restart:  unless-stopped
    build: .
    env_file: .env
    container_name: lokal-backend
    ports:
      - "127.0.0.1:8000:8000"
    depends_on:
      - redis
  redis:
    restart:  unless-stopped
    image: redis:8-alpine
    container_name: redis-lokal
